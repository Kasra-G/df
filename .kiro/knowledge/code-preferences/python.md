# Python Code Preferences

> Also apply preferences from `general.md`

- No trailing commas in function signatures and calls — remove them
- Use `from __future__ import annotations` for forward reference support
